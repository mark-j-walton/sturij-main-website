// The fallback the enquiry band shows when a post fails — never a silent loss (the brief, step 5).
export const CONTACT = {
  phoneDisplay: '01937 326011',
  phoneHref: 'tel:+441937326011',
  email: 'contact@sturij.com',
} as const
